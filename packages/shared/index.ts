/**
 * PlaneSpotter shared package entry point.
 * Author CKC. Version 0.1.0.
 *
 * Phase S, Block 3 adds tokens.ts, types.ts, events.ts and the zod schemas
 * per BuildPack v1.8 section 2.1. Only the registration validator exists today.
 */

export * from './registration-prefixes';
